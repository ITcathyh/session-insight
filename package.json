{
  "name": "session-insight",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "insight": "bash scripts/start-session-insight.sh",
    "insight:sync": "node scripts/sync-session-insight.mjs",
    "test:sync": "node --test scripts/sync-session-insight.test.mjs",
    "dev": "turbo dev --filter=@session-insight/app",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "lint": "turbo lint",
    "test:e2e": "pnpm --filter @session-insight/app test:e2e",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "packageManager": "pnpm@10.28.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ],
    "overrides": {
      "@types/react": "catalog:",
      "@types/react-dom": "catalog:"
    }
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "turbo": "^2.5.4",
    "typescript": "catalog:"
  }
}
